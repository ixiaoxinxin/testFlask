import os
from flask_forms import macros
import jinja2


def FlaskForms(app):
    extra_folders = jinja2.ChoiceLoader([
        app.jinja_loader,
        jinja2.FileSystemLoader(os.path.dirname(macros.__file__)),
    ])
    app.jinja_loader = extra_folders